/*Come up with a strategy before coding.
n is our limit.
declare var i=0.
we increment i by 1.
add i to the result variable.
stop when i ===n*/

function sum(n)[
    var result=0;
    var i=0;

    while (i<= n){
        result= result + n;
        i= i + 1;
    }
    return result;
    }
]
//